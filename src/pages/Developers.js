import { ChangeCategory } from "../components/ChangeCategory";

export const Developers = (props) => {
   

    return (
    <div>
       <ChangeCategory category={props.category} />
    </div>
    );
}